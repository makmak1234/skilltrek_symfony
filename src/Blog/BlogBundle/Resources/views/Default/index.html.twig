{% extends '::base.html.twig' %}

{% block body %}

	{% for post in posts %}
		<a href="{{ path('blog_blog_post', { id: post.id }) }}">
			<h2>{{ post.title }}</h2>
		</a>

		<p>{{ post.content }}</p>
	{% endfor %}

{% endblock %}
